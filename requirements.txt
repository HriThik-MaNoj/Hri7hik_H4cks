# Python dependencies for the Obsidian → Hugo converter.
# Keep this list lean — only what scripts/obsidian_to_hugo_converter.py imports.

pyyaml>=6.0      # YAML parsing (config + front matter)
Pillow>=9.0.0    # Image resize + JPEG/PNG optimisation
